<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
  <h4 class="modal-title">Delete user ({{ user.id }})</h4>
</div>
<div class="modal-body">
  <p>Are you sure you want to delete the user holding the ID of {{ user.id }}?</p>
  <p>Please check the details as shown in the table before continuing:</p>

  <table class="table">
    <thead>
    <tr>
      <th>User ID</th>
      <th>Email address</th>
      <th>Full name</th>
      <th>User role</th>
    </tr>
    </thead>

    <tbody>
    <tr>
      <td>{{ user.id }}</td>
      <td>{{ user.email }}</td>
      <td>{{ user.full_name }}</td>
      <td>{{ user.role }}</td>
    </tr>
    </tbody>
  </table>
</div>
<div class="modal-footer">
  {% autoescape false %}
    {{ form_open({'route': ['admin.users.destroy', user.id], 'method': 'delete'}) }}
      {{ form_button('Delete user', {'type': 'submit', 'class': 'btn btn-danger'}) }}
      <button class="btn btn-default" href="{{ route('admin.users.index') }}" data-dismiss="modal">Cancel</button>
    {{ form_close() }}
  {% endautoescape %}
</div>