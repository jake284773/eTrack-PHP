<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
  <h4 class="modal-title">Delete subject sector ({{ subjectSector.id }})</h4>
</div>
<div class="modal-body">
  <p>Are you sure you want to delete the subject sector ({{ subjectSector.name }})?</p>
  <p>Please check the details as shown in the table before continuing:</p>

  <table class="table">
    <thead>
    <tr>
      <th>Subject sector ID</th>
      <th>Subject sector name</th>
    </tr>
    </thead>

    <tbody>
    <tr>
      <td>{{ subjectSector.id }}</td>
      <td>{{ subjectSector.name }}</td>
    </tr>
    </tbody>
  </table>
</div>
<div class="modal-footer">
  {% autoescape false %}
    {{ form_open({'route': ['admin.subjectsectors.destroy', subjectSector.id], 'method': 'delete'}) }}
      {{ form_button('Delete subject sector', {'type': 'submit', 'class': 'btn btn-danger'}) }}
      <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
    {{ form_close() }}
  {% endautoescape %}
</div>